# WriteWeave Environment Variables

# Google Gemini API Key (required for AI features)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here

# Note: This API key will be embedded in the client-side bundle.
# For production use, consider implementing a backend proxy to keep the API key secure.
